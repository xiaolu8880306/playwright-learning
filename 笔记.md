# Playwright 学习笔记

---

## 一、安装与环境配置

### 1.1 安装步骤

```bash
# 1. 安装测试运行器（开发依赖）
npm install -D @playwright/test
# 注意：每个项目目录都需要安装

# 2. 下载三大浏览器内核及系统依赖
npx playwright install

# 3. 验证安装是否成功
npx playwright --version
# 输出示例：Version 1.48.0

# 4. 检查哪些浏览器已安装
npx playwright install --dry-run
```

### 1.2 代码生成器（录制工具）

```bash
# 打开录制工具，访问指定URL
npx playwright codegen https://demo.playwright.dev/todomvc/
# 会自动生成操作代码，适合快速上手
```

---

## 二、配置文件详解

配置文件 `playwright.config.ts` 是核心，定义在哪些浏览器跑、是否无头、视口大小、基础地址等。该文件要放到项目的根目录。

### 2.1 完整配置示例

```typescript
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './tests',  // 测试用例目录
  reporter: [['html', { open: 'never' }]],  // 报告配置

  // 失败重试配置
  retries: 2,  // 失败后重试2次（默认0，不重试）
  // CI环境可以设置更多次：retries: process.env.CI ? 3 : 0
  // 意思是：CI环境重试3次，本地不重试

  // 并行执行配置
  workers: 4,  // 最多同时跑4个测试（默认是CPU核心数的一半）
  // workers: 1 表示串行执行（调试时推荐）

  use: {
    baseURL: 'https://demo.playwright.dev/todomvc/',  // 基础URL
    headless: true,  // 无头模式，CI必开
    viewport: { width: 1280, height: 720 },  // 视口大小
    trace: 'on-first-retry',  // 失败重试时自动录trace
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
    {
      name: 'firefox',
      use: { ...devices['Desktop Firefox'] },
    },
    {
      name: 'webkit',
      use: { ...devices['Desktop Safari'] },
    },
  ],
});
```

**重试相关说明：**
- `retries: 2` 表示一个用例失败后最多重试2次，即总共执行3次
- 配合 `trace: 'on-first-retry'` 使用，只在重试时录trace，节省空间
- 配合 `video: 'on-first-retry'` 使用，只在重试时录视频
- 配合 `screenshot: 'on-first-retry'` 使用，只在重试时截图
- 本地调试时建议 `retries: 0`，CI环境建议 `retries: 2~3`

**命令行控制重试次数：**
```bash
# 命令行覆盖配置文件的重试次数
npx playwright test --retries=3

# 只跑上次失败的用例
npx playwright test --last-failed
```

### 2.2 可观测性配置

在配置文件的 `use` 中添加：

```typescript
use: {
  // 截图：on=每次都截，on-first-retry=失败重试时截，only-on-failure=仅失败时
  screenshot: 'on',
  fullPage: true,  // true=长截图，false=仅可视区域
  
  // 视频：on=每次都录，off=关闭，on-first-retry=失败重试时录，retain-on-failure=仅失败时保留
  video: 'on',
  
  // Trace：on=每次都录，on-first-retry=失败重试时录，retain-on-failure=仅失败时保留
  trace: 'on',
}
```

**视频 vs Trace 的区别：**
- **视频**：生成WEBM格式，整个测试过程的录制，只能看到画面，看不到DOM、网络请求等细节
- **Trace**：带DOM的快照，是个zip文件，包含DOM快照、网络请求、控制台日志、截图、时间线

```bash
# 查看trace文件
npx playwright show-trace test-results/trace.zip
```

---

## 三、常用命令

```bash
# 列出所有测试用例（测试用例是 .spec.ts 或 .test.ts 文件）
npx playwright test --list

# 跑所有测试（无头模式，默认）
npx playwright test

# 跑测试（有头模式，能看到浏览器）
npx playwright test --headed

# 跑指定文件
npx playwright test tests/demo.spec.ts

# 跑指定用例（按名称匹配）
npx playwright test -g "首页加载"

# 查看测试报告（默认失败时才生成）
npx playwright show-report
```

**报告配置说明：**
- Playwright默认只在失败时生成报告
- 想成功时也生成报告，需要在配置文件中加：`reporter: [['html', { open: 'never' }]]`

---

## 四、定位器

### 4.1 getByRole（角色定位）⭐⭐⭐⭐⭐

根据元素的"角色"来定位，模拟屏幕阅读器的视角。**最推荐，最稳定。**

```typescript
// 定位按钮
page.getByRole('button', { name: '提交' })

// 定位a链接
page.getByRole('link', { name: '首页' })

// 定位输入框
page.getByRole('textbox', { name: '用户名' })

// 定位复选框
page.getByRole('checkbox', { name: '记住我' })

// 定位下拉框
page.getByRole('combobox', { name: '选择城市' })

// 定位表格
page.getByRole('table')

// 定位标题（h1~h6）
page.getByRole('heading', { name: '用户信息' })

// 定位图片
page.getByRole('img', { name: 'logo' })

// 定位列表
page.getByRole('list')
page.getByRole('listitem')
```

**常用角色对照表：**

| HTML标签 | 对应role |
|----------|----------|
| `<button>` | button |
| `<a href="...">` | link |
| `<input type="text">` | textbox |
| `<input type="checkbox">` | checkbox |
| `<input type="radio">` | radio |
| `<select>` | combobox |
| `<h1>`~`<h6>` | heading |
| `<img>` | img |
| `<table>` | table |

### 4.2 getByText（文本定位）⭐⭐⭐⭐⭐

根据页面上用户可见的文本来定位。

```typescript
// 精确匹配（默认行为，元素文本必须完全等于指定字符串）
page.getByText('登录')

// 模糊匹配（包含即可，设置exact: false）
page.getByText('登录', { exact: false })
// 例如：页面上有"立即登录"和"登录"两个元素
// getByText('登录') 只匹配"登录"
// getByText('登录', { exact: false }) 两个都能匹配到

// 正则匹配（包含即可，天然模糊匹配）
page.getByText(/登录/)        // 匹配包含"登录"的文本
page.getByText(/登录/i)       // 忽略大小写
page.getByText(/^登录$/)      // 正则精确匹配（^开头 $结尾）
```

**注意：** 匹配的是用户能看到的文字，不是HTML里的属性值。

### 4.3 getByLabel（标签定位）⭐⭐⭐⭐

通过 `<label>` 标签的文本来定位对应的表单元素。**表单专用。**

```typescript
// HTML结构：
// <label for="username">用户名</label>
// <input id="username" type="text" />

// 定位方式：
page.getByLabel('用户名')

// 也支持嵌套label的情况：
// <label>
//   用户名
//   <input type="text" />
// </label>
page.getByLabel('用户名')
```

**适用场景：** 所有带label的表单输入框。

### 4.4 getByPlaceholder（占位符定位）⭐⭐⭐⭐

通过 input 的 placeholder 属性定位。

```typescript
// HTML: <input placeholder="请输入手机号" />
page.getByPlaceholder('请输入手机号')

// 模糊匹配
page.getByPlaceholder(/手机号/)
```

**适用场景：** 输入框有placeholder提示时。

### 4.5 getByTestId（测试ID定位）⭐⭐⭐⭐

通过 data-testid 属性定位，需要开发配合加属性。

```typescript
// HTML: <button data-testid="submit-btn">提交</button>
page.getByTestId('submit-btn')

// 自定义testId属性名（默认是data-testid）
// 在playwright.config.ts中配置：
// use: { testIdAttribute: 'data-test' }
// 那么就可以用 data-test="xxx" 来定位
```

**优点：** 不受UI文字变化影响，最稳定。  
**缺点：** 需要开发配合加属性。

### 4.6 getByTitle（标题定位）⭐⭐⭐

通过元素的 title 属性定位。

```typescript
// HTML: <button title="关闭弹窗">X</button>
page.getByTitle('关闭弹窗')
```

**适用场景：** 图标按钮等只有title没有文字的元素。

### 4.7 locator（CSS/XPath选择器）⭐⭐

当上面的定位器都不好用时，用CSS或XPath。**兜底方案。**

```typescript
// CSS选择器
page.locator('.class-name')           // 按class
page.locator('#id-name')              // 按id
page.locator('div > span')            // 按层级关系
page.locator('[data-value="123"]')    // 按属性
page.locator('input[type="password"]') // 按属性值
page.locator('.btn-primary')          // 按class

// XPath
page.locator('xpath=//button[@class="submit"]')
page.locator('xpath=//div[@id="app"]/form/input')

// 链式过滤
page.locator('.product-card').filter({ hasText: 'iPhone' })
page.locator('.list-item').nth(0)     // 第1个
page.locator('.list-item').last()     // 最后一个
```

---

## 五、定位器的链式操作

```typescript
// 过滤：在多个匹配结果中筛选
// hasText：包含字符串
page.getByRole('listitem').filter({ hasText: '产品A' })

// 第N个元素（从0开始）
page.locator('.item').first()    // 第1个
page.locator('.item').last()     // 最后1个
page.locator('.item').nth(2)     // 第3个

// 嵌套定位（在某个元素内部找）
page.locator('.modal').getByRole('button', { name: '确定' })

// has：必须包含某个子元素
// has必须是一个定位器对象，不是字符串（page.locator(...) 或 page.getByRole(...)）
page.locator('.card').filter({
  has: page.locator('.badge-new')
})

// hasNot：不能包含某个子元素
page.locator('.card').filter({
  hasNot: page.locator('.disabled')
})
```

---

## 六、定位器的操作

定位到元素后，可以执行这些操作：

```typescript
// 点击
await page.getByRole('button', { name: '提交' }).click()

// 双击
await page.getByRole('button').dblclick()

// 右键点击
await page.getByRole('button').click({ button: 'right' })

// 输入文本（先清空再输入）
await page.getByPlaceholder('用户名').fill('admin')

// 逐字输入（模拟真实打字，会触发键盘事件）
await page.getByPlaceholder('搜索').pressSequentially('hello', { delay: 100 })

// 清空输入框
await page.getByPlaceholder('用户名').clear()

// 勾选/取消复选框
await page.getByRole('checkbox').check()
await page.getByRole('checkbox').uncheck()

// 选择下拉框选项
await page.getByRole('combobox').selectOption('value1')
// 根据value选择：<option value="value1">北京</option>

await page.getByRole('combobox').selectOption({ label: '北京' })
// 根据label选择：<option value="value1">北京</option>

// 上传文件
// <input type="file">  <!-- 文件上传框 -->
await page.locator('input[type="file"]').setInputFiles('path/to/file.png')

// 悬停
await page.getByRole('link').hover()

// 获取文本内容
const text = await page.getByRole('heading').textContent()

// 获取属性值
const href = await page.getByRole('link').getAttribute('href')

// 判断是否可见
const isVisible = await page.getByRole('button').isVisible()

// 判断是否启用/禁用
const isEnabled = await page.getByRole('button').isEnabled()

// 等待元素出现
await page.getByText('加载完成').waitFor()
```

---

## 七、断言

```typescript
import { expect } from '@playwright/test';

// 元素可见
await expect(page.getByText('欢迎')).toBeVisible()

// 元素不可见
await expect(page.getByText('错误')).not.toBeVisible()

// 元素包含文本
await expect(page.getByRole('heading')).toHaveText('用户中心')

// 输入框的值
await expect(page.getByPlaceholder('用户名')).toHaveValue('admin')

// 元素数量
await expect(page.locator('.list-item')).toHaveCount(5)

// 元素包含某个class
await expect(page.getByRole('button')).toHaveClass(/active/)

// 元素属性
await expect(page.getByRole('link')).toHaveAttribute('href', '/home')

// 元素启用/禁用
await expect(page.getByRole('button')).toBeEnabled()
await expect(page.getByRole('button')).toBeDisabled()

// 元素获得焦点
await expect(page.getByPlaceholder('搜索')).toBeFocused()

// 页面标题
await expect(page).toHaveTitle(/STORE/)
```

---

## 八、Page Object 模式（代码结构）

### 8.1 页面对象封装（pages/HomePage.ts）

```typescript
// 固定写法，导入Page类型
import { Page, Locator } from '@playwright/test';

// export导出这个类，让别的文件导入，不写的话只能文件内部使用
export class HomePage {
    // 构造函数，接收page参数并保存为私有属性
    // 当在测试用例中new HomePage(创建对象时传来的东西)，会自动调用这个函数
    // private：这是TypeScript的参数属性，会自动声明一个私有字段page（就是自定义的参数名），把构造函数传进来的参数复制给它
    // 参数名是page，类型是Page，能干goto、点击、填表单，等等所有操作
    // 整个意思是：创建HomePage对象时，必须传过来一个浏览器页面
    constructor(private page: Page) {}
    
    // async：固定写法，表示异步操作，浏览器操作都要等
    async goto() {
        // 访问根路径。在playwright.config.ts里配置了baseURL
        // await：是一个异步操作，要等浏览器真正点完，等它执行完再往下走
        await this.page.goto('/');
    }
    
    // 自己写的，定义一个方法
    // 参数是：name，string类型，接受分类名称
    async openCategory(name: string) {
        // playwright定位器，找到文本是name（调用函数的地方传来的名字）的链接，进行点击
        // getByRole是角色定位器，意思是在页面上找一个role为link的元素
        // a标签role：link
        // button标签role：button
        // input标签role：textbox
        await this.page.getByRole('link', { name }).click();
    }
    
    // 定义一个方法
    async addFirstProductToCart() {
        // 根据CSS选择器找商品标题，.first()取第一个进行点击
        // locator是CSS选择定位元素，.card-title是CSS选择器语法，意思是找class名叫card-title的元素，id是#
        await this.page.locator('.card-title').first().click();
        
        // 点击按钮add to cart
        // 'link', { name: 'Add to cart' }，这是两个参数
        // 参数1：'link'元素的role，限定只找a标签
        // 参数2：{ name: 'Add to cart' }，找到名字是add to cart的元素，name也就是用户在页面可以看到的字
        await this.page.getByRole('link', { name: 'Add to cart' }).click();
    }
}
```

### 8.2 测试用例（tests/demo.spec.ts）

```typescript
// 这是固定写法，每个测试文件都要写这，导入这两个东西
// test：用来定义测试用例
// expect：用来写断言的
import { test, expect } from '@playwright/test';

// 导入创建的页面对象，路径是../pages/HomePage的文件下的类HomePage
import { HomePage } from '../pages/HomePage';

// test('测试用例名字')
// async({page}) => {}: 固定写法，page是Playwright自动提供的浏览器页面对象
test('首页加载并加购商品', async ({ page }) => {
    // 自己写的，创建页面对象实例。把playwright提供的page传给HomePage，命名为home
    const home = new HomePage(page);
    
    // 调用定义的方法，goto，就是在HomePage.ts里写的，作用是打开网站
    await home.goto();
    
    // 固定写法：断言，验证页面的标题包含"STORE"（正则表达式写法/STORE/）
    await expect(page).toHaveTitle(/STORE/);
    
    // home.openCategory：调用HomePage里面的方法，点击"Laptops"分类
    await home.openCategory('Laptops');
    
    // 调用HomePage里面的方法addFirstProductToCart
    await home.addFirstProductToCart();
    
    // 固定写法，监听弹窗，当页面弹出alert时，自动点击确认
    page.once('dialog', d => d.accept());
});
```

---

## 九、定位器选择策略总结

| 场景 | 推荐定位器 | 示例 |
|------|-----------|------|
| 按钮 | getByRole('button') | getByRole('button', { name: '提交' }) |
| 链接 | getByRole('link') | getByRole('link', { name: '首页' }) |
| 输入框（有label） | getByLabel | getByLabel('用户名') |
| 输入框（有placeholder） | getByPlaceholder | getByPlaceholder('请输入') |
| 文本内容 | getByText | getByText('欢迎') |
| 开发配合 | getByTestId | getByTestId('submit-btn') |
| 复杂结构 | locator + filter | locator('.card').filter({ hasText: 'A' }) |
| 兜底 | locator(CSS) | locator('.btn-primary') |

**核心原则：** 能用 getByRole/getByText 就别用 locator(CSS)，前者模拟用户视角更稳定，后者容易因UI改版失效。
